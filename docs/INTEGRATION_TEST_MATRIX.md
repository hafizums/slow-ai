# Integration Test Matrix

| Feature | Boundary tested | Real DocTypes | Real API | Real worker | Real provider | Expected records | Command |
|---|---|---:|---:|---:|---:|---|---|
| Platform kernel | DocType persistence | Yes | No | No | No | All core DocTypes | bench --site saas run-tests --app slow_ai |
| Core DocTypes | DocType schema + DB inserts | Yes | No | No | No | All permanent DocTypes and relationships | bench --site saas run-tests --app slow_ai |
| Workflow JSON + Node Registry | Domain validation + API metadata | No | Yes | No | No | Valid graph, object_info, initial nodes | bench --site saas run-tests --app slow_ai |
| Save workflow | API + DB | Yes | Yes | No | No | AI Workflow | bench --site saas run-tests --app slow_ai |
| Start run | API + service + DB | Yes | Yes | No | No | Workflow Version, Workflow Run, Node Runs | bench --site saas run-tests --app slow_ai |
| Run preflight | API + application spend/model/account policy | Yes | Yes | No | No | Rejects before Workflow Version, Run, Node Runs, Provider Job, queue | bench --site saas run-tests --app slow_ai |
| Billing credit balance | AI Credit Ledger + billing APIs + preflight balance policy | Yes | Yes | No | No | CREDIT top-up, balance totals, insufficient-balance rejection before run/version/node/provider-job/queue, actual-cost DEBIT, estimated fallback DEBIT, idempotent DEBIT, zero-cost/failed no-debit, safe ledger history | bench --site saas run-tests --app slow_ai |
| Model catalog admin | AI Model persistence + safe metadata APIs + preflight model policy | Yes | Yes | No | No | Safe model metadata, disabled/unpriced/mismatched model rejection, WaveSpeed seed records, no Provider Job during catalog actions | bench --site saas run-tests --app slow_ai |
| Multi-provider foundation | Provider registry + generic provider nodes + model/account/preflight/billing boundaries | Yes | Yes | Yes | No | Multiple registered adapters, default/configured Provider Account on Provider Job, canonical AI Model link, mismatch rejection before enqueue, no engine provider imports | bench --site saas run-tests --app slow_ai |
| Replicate provider | First real second provider adapter + generic provider pipeline | Yes | Yes | Yes | No | Default registry includes Replicate, catalog seed, BYOK secret-safe account, ProviderJob before submit with estimate, Asset and estimated Ledger through generic materialization when actual cost is missing, bad combinations rejected before enqueue | bench --site saas run-tests --app slow_ai |
| BYOK provider accounts | Safe Provider Account APIs + scoped preflight/account resolution | Yes | Yes | Yes | No | Password-field secret storage, safe account payloads, scoped default/configured account on Provider Job, inactive/mismatched/disallowed rejection before enqueue, no provider calls during CRUD/preflight | bench --site saas run-tests --app slow_ai |
| Project membership | AI Project Member + central project access policy | Yes | Yes | No | No | OWNER/EDITOR/VIEWER/BILLING role checks, scoped workflow/run/asset/share/billing/provider-account access, non-member denial, insufficient-balance rejection before run/version/node/provider-job side effects | bench --site saas run-tests --app slow_ai; npm run test:e2e |
| Status, history, queue, assets | API + application queries | Yes | Yes | No | No | Run status, history rows, queue rows, uploaded Asset | bench --site saas run-tests --app slow_ai |
| Engine core | Application + engine + DB | Yes | No | Yes | No | Workflow Version, Run, Node Runs, outputs, errors | bench --site saas run-tests --app slow_ai |
| Graph cycle rejection | Domain validation | Yes | Yes | No | No | Error only | bench --site saas run-tests --app slow_ai |
| Worker execution | Worker + engine | Yes | No | Yes | No | Node Runs, Assets | bench --site saas run-tests --app slow_ai |
| Workers + realtime | Worker entrypoints + Frappe realtime log | Yes | No | Yes | No | Run/Node/Provider status events, scheduled provider polling batch | bench --site saas run-tests --app slow_ai |
| Asset and ledger pipeline | Provider poll worker + output materialization | Yes | No | Yes | No | Provider Job with estimate, Asset, actual-or-estimated Ledger, resumed downstream output | bench --site saas run-tests --app slow_ai |
| Canvas placeholder | Frappe Desk Page + API-only workflow flow | Yes | Yes | No | No | Page asset, Workflow, Version, Run, Node Runs, Asset view | bench --site saas run-tests --app slow_ai |
| Canvas run safety | Frappe Desk Page + safe model metadata API | Yes | Yes | No | No | Provider-node confirmation text, public model pricing metadata, backend-only start_run path | bench --site saas run-tests --app slow_ai |
| Object info node palette | Frappe Desk Page + object_info metadata | Yes | Yes | No | No | Metadata categories, schema summaries, Add Node action, backend preflight on provider run | bench --site saas run-tests --app slow_ai |
| Canvas graph editor | Frappe Desk Page + object_info-driven draft editor | Yes | Yes | No | No | Metadata config panel, node/edge draft edits, layout persistence, backend graph validation rejection | bench --site saas run-tests --app slow_ai |
| Canvas run monitor | Frappe Desk Page + run status/history APIs + realtime subscriptions | Yes | Yes | No | No | Workflow status, Node Run status, Provider Job status, Asset view links, Credit Ledger summary, safe errors, timeline | bench --site saas run-tests --app slow_ai |
| Canvas asset preview | Frappe Desk Page + asset view API | Yes | Yes | No | No | Image/video/audio/text/JSON preview branches, source metadata, Open/Copy/Refresh asset controls, API-derived links | bench --site saas run-tests --app slow_ai |
| Canvas template library | Frappe Desk Page + template APIs | Yes | Yes | No | No | Template list, saved Workflow Template, editable Workflow created from template, no run/version/provider job during template actions | bench --site saas run-tests --app slow_ai |
| Template publish review | AI Workflow Template + application review services + canvas controls | Yes | Yes | No | No | DRAFT/IN_REVIEW/PUBLISHED/REJECTED/ARCHIVED lifecycle, direct save cannot submit/publish/archive/reject, owner submit, System Manager approve/reject/archive, publication validation, public tools PUBLISHED-only, no execution/provider side effects | bench --site saas run-tests --app slow_ai; npm run test:e2e |
| Template versioning and rollback | AI Workflow Template Version + public template services + canvas controls | Yes | Yes | No | No | Approval creates immutable active version, prior version superseded, public tools read active version not mutable JSON, rollback creates new active version from historical snapshot, no workflow/run/provider/asset/ledger side effects | bench --site saas run-tests --app slow_ai; npm run test:e2e |
| Canvas Tool Mode form | Frappe Desk Page + template/workflow/run/asset APIs | Yes | Yes | No | No | Template-selected form, editable Workflow draft, saved form values, uploaded/selected AI Asset, backend-only start_run, backend preflight on provider templates | bench --site saas run-tests --app slow_ai |
| Public Tool Page and Run Library | User-facing Desk Page + published template/workflow/run/asset APIs | Yes | Yes | No | No | Published templates only, normal-user project access, editable Workflow draft, saved form values, backend-only start_run, rerun draft from recorded immutable template version, schema-only rerun prefill, scoped My Runs listing/detail, sanitized failed errors, no raw provider payloads, insufficient-balance rejection before ProviderJob, asset view output previews | bench --site saas run-tests --app slow_ai; npm run test:e2e |
| Template input schema | AI Workflow Template input_schema_json + backend prepare validation | Yes | Yes | No | No | Typed schema save, target field validation, unsafe provider/account/secret target rejection, required/select/number/asset validation, inaccessible asset rejection, editable Workflow draft only, no version/run/node/provider-job/asset/ledger side effects during prepare, EDITOR allowed and VIEWER rejected | bench --site saas run-tests --app slow_ai; npm run test:e2e |
| Run Output Gallery | Reusable safe output gallery service + public tool API/page rendering | Yes | Yes | No | No | Grouped safe AI Asset previews from backend asset view data, VIEWER/EDITOR allowed, nonmember rejected, failed/empty safe empty payload, guest nested gallery run strips project/internal workflow metadata, no provider account/secrets/raw provider JSON, no ProviderJob/Asset/Ledger/Version/Run/NodeRun creation during gallery reads, shared page selected assets only | bench --site saas run-tests --app slow_ai; npm run test:e2e |
| Tool Run Sharing | Read-only share DocType + guest-safe shared page/API | Yes | Yes | No | No | AI Tool Run Share with selected_assets_json, owner/System Manager manage rules, non-empty selected assets, cross-run/unknown asset rejection, guest active-token read, selected-only asset previews, disabled/expired rejection, no run/version/provider-job/asset/ledger creation during share actions | bench --site saas run-tests --app slow_ai; npm run test:e2e |
| Provider account UI | Frappe Desk Page + safe provider account APIs | Yes | Yes | No | No | Create/list/view/default/disable BYOK accounts, password not displayed after save, safe payloads only, disabled account rejected by preflight, no Provider Job during CRUD/preflight | bench --site saas run-tests --app slow_ai; npm run test:e2e |
| Model catalog UI | Frappe Desk Page + safe model catalog APIs | Yes | Yes | No | No | Safe model list/detail metadata, provider/status/node_type/category filters, disabled/unpriced warnings, admin status/pricing updates, no Provider Job during catalog actions | bench --site saas run-tests --app slow_ai; npm run test:e2e |
| Private workspace | Frappe Desk Workspace + user sidebar | Yes | No | No | No | Private Workspace for system user, canvas and DocType links | bench --site saas run-tests --app slow_ai |
| Tool mode foundation | Node registry + template APIs + engine persistence | Yes | Yes | Yes | No | Tool output Node Run, Workflow Template, instantiated Workflow | bench --site saas run-tests --app slow_ai |
| Review and hardening | Static architecture boundary gate under real Frappe test runner | No | Yes | No | No | Thin API delegates, persistence-only DocTypes, API-only client, no direct SQL/local runtime/ComfyUI production references | bench --site saas run-tests --app slow_ai |
| Provider base + WaveSpeed skeleton | Provider adapter + DB lifecycle | Yes | No | No | No | Provider Job submit/poll state and error payloads | bench --site saas run-tests --app slow_ai |
| Provider nodes | Node registry + engine + provider adapter boundary | Yes | No | Yes | No | Provider Job, Asset, Ledger, Node Run provider link | bench --site saas run-tests --app slow_ai |
| Provider submit | Provider adapter | Yes | No | Yes | Yes | Provider Job | SLOW_AI_REAL_PROVIDER_TESTS=1 ... |
| Provider output asset | Provider + asset pipeline | Yes | No | Yes | Yes | Provider Job, Asset, Ledger | SLOW_AI_REAL_PROVIDER_TESTS=1 ... |
| Real WaveSpeed workflow | Full application + worker + WaveSpeed API path | Yes | Yes | Yes | Yes | Workflow Version, Run, Node Runs, Provider Job before submit, Asset on success, Ledger on non-zero cost, history rows | SLOW_AI_REAL_PROVIDER_TESTS=1 WAVESPEED_API_KEY=xxx bench --site saas run-tests --app slow_ai |
| Real WaveSpeed invalid key | WaveSpeed adapter failure path | Yes | No | No | Yes | Failed Provider Job, persisted request/response/error, no Asset or Ledger | SLOW_AI_REAL_PROVIDER_TESTS=1 WAVESPEED_API_KEY=xxx bench --site saas run-tests --app slow_ai |
| Real Replicate workflow | Full application + worker + Replicate API path | Yes | Yes | Yes | Yes | Workflow Version, Run, Node Runs, Provider Job before submit with estimate, Asset on success, actual-cost Ledger when Replicate returns cost or estimated Ledger when it does not, history rows | SLOW_AI_REAL_REPLICATE_TESTS=1 REPLICATE_API_KEY=xxx bench --site saas run-tests --app slow_ai |
| Real Replicate invalid key | Replicate adapter failure path | Yes | No | No | Yes | Failed Provider Job, persisted request/response/error, no Asset or Ledger | SLOW_AI_REAL_REPLICATE_TESTS=1 REPLICATE_API_KEY=xxx bench --site saas run-tests --app slow_ai |
| Run history | API + DB | Yes | Yes | No | No | Run, Node Runs, Provider Jobs, Assets | bench --site saas run-tests --app slow_ai |
| Canvas save | UI + API | Yes | Yes | No | No | AI Workflow | npm run test:e2e |
| Canvas browser E2E | Frappe Desk browser + API + persisted docs | Yes | Yes | No | No | Login, object_info palette, node/config edits, edge add/delete, saved Workflow, provider confirmation, run status/history, asset cards | npm run test:e2e |
| Visual canvas browser E2E | Frappe Desk visual graph layer + API + persisted docs | Yes | Yes | No | No | Metadata-rendered nodes, drag-to-layout, visual edge delete/create, saved layout, backend-only save/start | npm run test:e2e |
| Tool Mode browser E2E | Frappe Desk browser + template/workflow/run/asset APIs | Yes | Yes | No | No | Template load, editable Workflow draft, saved form values, selected/uploaded AI Asset, backend-only start_run | npm run test:e2e |
| Public Tool browser E2E | Frappe Desk browser + public template/workflow/run/asset APIs | Yes | Yes | No | No | Normal user login, /app/slow-ai-tools, published template selection, text/upload form values, backend-only start_run, My Runs list/detail, safe output asset cards | npm run test:e2e |
| Project Membership browser E2E | Frappe Desk browser + safe project membership APIs | Yes | Yes | No | No | Owner adds EDITOR and VIEWER members, editor can use project through Tool Mode, viewer can view runs but cannot create/start a run, no provider requests/secrets | npm run test:e2e |
| Run Output Gallery browser E2E | Frappe Desk + guest shared page | Yes | Yes | No | No | Normal user opens My Runs detail with multi-asset output gallery, image/video/audio cards render, one selected asset is shared, guest sees selected asset only, no Run button/provider requests/secrets | npm run test:e2e |
| Tool Run Sharing browser E2E | Frappe Desk + guest website route | Yes | Yes | No | No | Normal user selects one output in My Runs detail, creates share link, guest opens read-only shared page, selected asset visible, unselected asset hidden, no Run button, no provider requests/secrets | npm run test:e2e |
| Model Catalog browser E2E | Frappe Desk browser + model APIs | Yes | Yes | No | No | Model Catalog opens, models list, model detail loads, disabled/unpriced warnings visible, no provider requests or secrets | npm run test:e2e |
